{
  "name": "sparktool",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "22.22.2"
  },
  "packageManager": "pnpm@9.14.4",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "clean": "pnpm -r --if-present clean",
    "tunnel": "ngrok http 4417"
  },
  "devDependencies": {
    "ngrok": "5.0.0-beta.2",
    "turbo": "^2.5.6"
  }
}
